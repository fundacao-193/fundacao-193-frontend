/**
 * Minimal Worker entry point
 * Wrangler will serve static files from [site] bucket automatically
 */

export default {
  fetch() {
    // Static files are served by Wrangler's built-in middleware
  },
};
